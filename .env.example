# Optional self-hosted AI configuration. Keep actual secrets out of Git and the browser.
OPENAI_API_KEY=
# Set an explicitly chosen model available to your OpenAI project with Structured Outputs.
OPENAI_MODEL=
PORT=8787
